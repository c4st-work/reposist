#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct Node{
    int value;
    struct Node *next;
}Node;

Node *makenode(int v){
    Node *p= (Node *)malloc(sizeof(Node));
    p->value=v;
    p->next=NULL;
    return p;
}

void print(Node *h){
    Node *p=h;
    while(p!=NULL){
        printf("%d ", p->value);
        p=p->next;
    }
}

Node *find(Node *head, int val){
    Node *p=head;
    while(p!=NULL){
        if(p->value==val){
            return p;
        }
        p=p->next;
    }
    return NULL;
}

Node *insert(Node *head, int v){
    Node *makenode(v);
    if(head==NULL){
        return makenode(v);
    } else {
        makenode->next=head;
        head=makenode;
    }
    return head;
}

Node *findlast(Node *head){
    Node *p=head;
    while(p!=NULL){
        if(p->next==NULL){
            reuturn p;
        }
        p=p->next;
    }
    return NULL;
}

Node *insertlast(Node *findlast, int v){
    Node *makenode(v);
    if(findlast==NULL){
        return makenode(v);
    } else {
        Node *lastnode=findlast(head);
        findlast->next=makenode;
    }
}

int main(){
    Node *head, *node1, *node2;
    head=makenode(10);
    node1=makenode(20);
    node2=makenode(30);
    head->next=node1;
    node1->next=node2;
    print(list);
}
